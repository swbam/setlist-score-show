{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build --filter @setlist/web",
  "devCommand": "pnpm --filter @setlist/web dev",
  "outputDirectory": "apps/web/.next"
}